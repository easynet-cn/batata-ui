# Development environment configuration

# API base URL prefix (maps to console server's nacos.console.contextPath)
# Console server default context path is empty, so no prefix needed
VITE_API_BASE_URL=

# Backend console server address (nacos.console.port, default 8081)
# Frontend always connects to the console server, never directly to the main server (8848)
VITE_API_PROXY_TARGET=http://localhost:8081

# Consul server address (proxy target, not exposed to browser)
VITE_CONSUL_PROXY_TARGET=http://localhost:8500

# Apollo Portal server address (proxy target, not exposed to browser)
VITE_APOLLO_PROXY_TARGET=http://localhost:8080
